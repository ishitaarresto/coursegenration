# Content Ingestion module dependencies

# PDF extraction — PyMuPDF; imported as `fitz`
PyMuPDF>=1.24.0

# DOCX extraction
python-docx>=1.1.0

# PPTX extraction
python-pptx>=0.6.23

# Image processing (slide/page thumbnails, embedded image handling)
Pillow>=10.3.0

# BLIP image captioning
# torch installation varies by platform — install the CPU wheel from PyPI or
# get the CUDA build from https://pytorch.org/get-started/locally/
#   CPU (all platforms):  pip install torch
#   CUDA 12.x (Linux/Win): pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
transformers>=4.37.0

# Chunking + embedding
sentence-transformers>=2.7.0

# Vector store
chromadb>=0.4.0

name: lms_app
description: AI-Powered LMS — Author Studio (Course Generation)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  url_launcher: ^6.3.2
  web: ^1.1.1
  webview_flutter: ^4.13.1
  webview_flutter_web: ^0.2.3

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

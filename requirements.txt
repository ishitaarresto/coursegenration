# Arresto LMS — backend dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Database — SQLAlchemy ORM + PostgreSQL driver
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# HTTP client (Sarvam TTS, HeyGen API)
httpx>=0.27.0

# AI / LLM
anthropic>=0.28.0

# Document parsing
PyMuPDF>=1.24.0
python-docx>=1.1.0
python-pptx>=0.6.23
Pillow>=10.3.0

# Embeddings + vector store
sentence-transformers>=2.7.0
chromadb>=0.4.0
rank-bm25>=0.2.2

# Optional — image captioning (BLIP-2, slow on CPU)
# torch>=2.0.0
# transformers>=4.37.0

# OCR for scanned PDFs (pure Python, no system binary required)
easyocr>=1.7.0

# Edge TTS (free text-to-speech fallback)
edge-tts>=6.1.9

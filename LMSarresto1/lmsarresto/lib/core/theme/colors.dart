import 'package:flutter/material.dart';

class AColors {
  AColors._();

  // ── Brand ─────────────────────────────────────────────────────
  static const amber      = Color(0xFFF5BE3F);
  static const amberDark  = Color(0xFFD4A017);  // pressed / hover amber
  static const orange     = Color(0xFFC2410C);
  static const ink        = Color(0xFF1B1B1D);

  // ── Backgrounds — cooler slate for outdoor screen readability ──
  static const bg         = Color(0xFFF1F2F4);  // was warm cream #F7F5F2
  static const bg2        = Color(0xFFE8E9ED);  // was #F0EDE8
  static const surface    = Color(0xFFFFFFFF);
  static const surfaceSoft= Color(0xFFF8F8FA);  // was #FAF9F7

  // ── Borders — stronger contrast ────────────────────────────────
  static const cardBorder = Color(0xFFD8D9DF);  // was #E8E4DE
  static const line       = Color(0xFFD8D9DF);
  static const lineStrong = Color(0xFFC4C5CC);  // was #D4CFC8

  // ── Text ───────────────────────────────────────────────────────
  static const textPrimary= Color(0xFF1B1B1D);
  static const textSecond = Color(0xFF3D3A36);
  static const textMuted  = Color(0xFF71717A);
  static const textMuted2 = Color(0xFFA1A1AA);

  // ── Semantic ───────────────────────────────────────────────────
  static const green      = Color(0xFF1F8A5B);
  static const greenSoft  = Color(0xFFDCFCE7);
  static const red        = Color(0xFFD1432F);
  static const redSoft    = Color(0xFFFEE2E2);
  static const blue       = Color(0xFF2563B8);
  static const blueSoft   = Color(0xFFDBEAFE);
  static const amberSoft  = Color(0xFFFEF3C7);
  static const orangeTint = Color(0xFFFFF1EC);

  // ── Sidebar ────────────────────────────────────────────────────
  static const sidebarDivider = Color(0xFF2A2A2D);
}
